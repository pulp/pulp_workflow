Expanded `Workflow` list filters via a `BaseFilterSet` subclass: `pulp_href`,
`pulp_href__in`, `pulp_id__in`, `prn__in`, `q` expression filtering,
datetime range filtering on `pulp_created`/`start_time`/`started_at`/
`finished_at`, and the full set of `NAME_FILTER_OPTIONS` for `name`.
